modules = ["nodejs-20", "web"]
run = "npm run dev"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "node server/dist/index.js"]
build = ["sh", "-c", "npm run build && cd server && npm run build"]
